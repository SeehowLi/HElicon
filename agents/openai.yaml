interface:
  display_name: "HElicon"
  short_description: "Project-aware FHE/security-paper writing mentor with H-* short commands."
  default_prompt: "$HElicon\nH-HELP: show the project-aware short commands and recommend the right next step."
policy:
  allow_implicit_invocation: true
